<?xml version="1.0" encoding="utf-8"?><!--
   API 31+ のバックアップ／端末間転送ルール。
   Room DB（database/）・写真（files/food_photos）・DataStore（files/datastore）を
   クラウドバックアップと端末間転送の両方から除外する。
   ユーザーの観測記録はこの端末内で完結させる（MVP の方針。書き出し・取り込みは将来の別機能）。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
